{
  "name": "functions",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "deploy": "firebase deploy --only functions",
    "dev": "firebase emulators:start --only functions,firestore,storage"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "firebase-admin": "^14.0.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^173.0.0",
    "sharp": "^0.35.1"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.19.43",
    "typescript": "6.0.3"
  }
}
